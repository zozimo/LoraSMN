Comando Git:

Clonar Repo

git clone https//github.com ....(ojo con CTRL v)

%mirar todos los logs
git log

% subir algo
git add .(add "nombres de archivos")
git status
git push origin main(branch main)
